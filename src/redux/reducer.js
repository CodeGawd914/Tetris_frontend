const initialState ={
  test: "testing"

}

const tetrisReducer = (state = initialState, action) => {
  switch (action.type) {
    case "":

    default:
    return state

  }
}

export default tetrisReducer
